import { Inter } from "next/font/google";
import { PT_Sans } from "@next/font/google";

export const inter = Inter({ subsets: ["latin"] });

export const pt_sans = PT_Sans({ subsets: ["latin"], weight: ["400", "700"] });